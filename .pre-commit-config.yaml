# See https://pre-commit.com for more information
# See https://pre-commit.com/hooks.html for more hooks
repos:
-   repo: https://github.com/pre-commit/pre-commit-hooks
    rev: 'v4.6.0'  # Updated to latest version
    hooks:
      - id: check-merge-conflict
      - id: debug-statements
      - id: end-of-file-fixer
      - id: mixed-line-ending
      - id: check-yaml
      - id: check-toml
      - id: check-json
      - id: trailing-whitespace

        #-   repo: https://github.com/pre-commit/mirrors-autopep8
        #    rev: ''
        #    hooks:
        #      - id: autopep8

### # flake8 --show-source <file>
### -   repo: https://gitlab.com/pycqa/flake8
###     rev: ''
###     hooks:
###       - id: flake8
#
# flake8 linting
-   repo: https://github.com/pycqa/flake8
    rev: '7.1.1'  # Use latest stable version
    hooks:
      - id: flake8
        args: ['--max-line-length=120', '--max-complexity=10']

-   repo: https://github.com/pre-commit/pygrep-hooks
    rev: 'v1.10.0'  # Updated to latest version
    hooks:
      - id: python-no-eval
      - id: python-no-log-warn
      - id: python-check-blanket-noqa

# mypy - static type checking
-   repo: https://github.com/pre-commit/mirrors-mypy
    rev: 'v1.11.0'  # Updated to latest version
    hooks:
      - id: mypy
        args: [ "--ignore-missing-imports", "--scripts-are-modules" ]
        additional_dependencies: [ types-requests ]

default_language_version:
    python: python3

default_stages: [ pre-commit, pre-push ]

#files: '([^\.].+/)*.py'
